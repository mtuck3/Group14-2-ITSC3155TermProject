<h1>Welcome, Task controller</h1>

  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.description %></td>
      <td><%= task.name %></td>
      <!--<td><%= link_to 'Show', article_path(article) %></td>-->
      <!--<td><%=link_to 'Edit', edit_article_path(article) %></td>-->
      <!--<td><%= link_to 'Destroy',-->
      <!--    article_path(article),-->
      <!--    method: :delete,-->
      <!--    data: {confirm: 'Are you sure?'}-->
      <!--    %></td>-->
      <!--  </td>-->
    </tr>
  <% end %>